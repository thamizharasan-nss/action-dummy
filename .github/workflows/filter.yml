name: event filter
on:
  push:
    branches: [ main] 
    #branches-ignore: 
     # - feature/*
  pull_request: 
    types: 
      - opened
      - closed
    paths-ignore: 
      - README.md
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: event fileter
        run: echo ${{ github.event_name }}
    
  
