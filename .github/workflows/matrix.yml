name: using the matrix option
on:
  push:
  workflow_dispatch:
jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        os: [ubuntu-latest, windows-latest]
        exclude:
          - os: windows-latest
        include:
          - os: macos-latest
    runs-on: ${{ matrix.os }} 
    steps:
    - name: docker information
      run: docker info  
